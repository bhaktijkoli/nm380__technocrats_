import React from 'react';
import Layout from '../layout';
import VideoList from '../components/videolist';

const Home = () => {
    return (
        <Layout>
            <VideoList />
        </Layout>
    );
};

export default Home;
